
==================================================
Código: 3 + 4;
==================================================
  NUMBER       → 3
  +            → '+'
  NUMBER       → 4
  ;            → ';'

==================================================
Código: let base : Int = 10;
==================================================
  LET          → 'let'
  ID           → 'base'
  :            → ':'
  TINT         → 'Int'
  =            → '='
  NUMBER       → 10
  ;            → ';'

==================================================
Código: let ativo : Bool = true;
==================================================
  LET          → 'let'
  ID           → 'ativo'
  :            → ':'
  TBOOL        → 'Bool'
  =            → '='
  TRUE         → 'true'
  ;            → ';'

==================================================
Código: fun dobro : Int -> Int;
==================================================
  FUN          → 'fun'
  ID           → 'dobro'
  :            → ':'
  TINT         → 'Int'
  ARROW        → '->'
  TINT         → 'Int'
  ;            → ';'

==================================================
Código: let dobro n = n * 2;
==================================================
  LET          → 'let'
  ID           → 'dobro'
  ID           → 'n'
  =            → '='
  ID           → 'n'
  *            → '*'
  NUMBER       → 2
  ;            → ';'

==================================================
Código: if n < 0 then 0 - n else n;
==================================================
  IF           → 'if'
  ID           → 'n'
  <            → '<'
  NUMBER       → 0
  THEN         → 'then'
  NUMBER       → 0
  -            → '-'
  ID           → 'n'
  ELSE         → 'else'
  ID           → 'n'
  ;            → ';'

==================================================
Código: -- comentário de linha
let x : Int = 1;
==================================================
  LET          → 'let'
  ID           → 'x'
  :            → ':'
  TINT         → 'Int'
  =            → '='
  NUMBER       → 1
  ;            → ';'

==================================================
Código: {- comentário
   multi-linha -}
let y : Bool = false;
==================================================
  LET          → 'let'
  ID           → 'y'
  :            → ':'
  TBOOL        → 'Bool'
  =            → '='
  FALSE        → 'false'
  ;            → ';'

==================================================
Código: 3 == 3;
==================================================
  NUMBER       → 3
  EQ           → '=='
  NUMBER       → 3
  ;            → ';'

==================================================
Código: 3 != 4;
==================================================
  NUMBER       → 3
  NEQ          → '!='
  NUMBER       → 4
  ;            → ';'

==================================================
Código: 3 <= 5;
==================================================
  NUMBER       → 3
  LE           → '<='
  NUMBER       → 5
  ;            → ';'

==================================================
Código: true && false;
==================================================
  TRUE         → 'true'
  AND          → '&&'
  FALSE        → 'false'
  ;            → ';'

==================================================
Código: true || false;
==================================================
  TRUE         → 'true'
  OR           → '||'
  FALSE        → 'false'
  ;            → ';'

==================================================
Código: when (n) is
  0 -> true;
  _ -> false;
end;
==================================================
  WHEN         → 'when'
  (            → '('
  ID           → 'n'
  )            → ')'
  IS           → 'is'
  NUMBER       → 0
  ARROW        → '->'
  TRUE         → 'true'
  ;            → ';'
  _            → '_'
  ARROW        → '->'
  FALSE        → 'false'
  ;            → ';'
  END          → 'end'
  ;            → ';'